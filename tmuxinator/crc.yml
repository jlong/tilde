name: crc
root: ~/Workspaces/crc-licensing

windows:
  - bash:
      layout: even-horizontal
      panes:
        - bash:
          - cls
        - git:
          - gitsh
          - cls
  - vim: vim
  - server: rails server
