name: devsite
root: ~/Workspaces/uservoice/devsite

windows:
  - bash:
      layout: even-horizontal
      panes:
        - bash:
          - cls
        - git:
          - gitsh
          - cls
  - vim: vim
  - server: rake preview
